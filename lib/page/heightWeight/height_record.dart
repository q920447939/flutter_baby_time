import 'package:flutter/material.dart';

class HeightRecord extends StatelessWidget {
  const HeightRecord({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('身高记录');
  }
}
