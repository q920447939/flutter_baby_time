import 'package:flutter/material.dart';

class UploadMainPage extends StatelessWidget {
  const UploadMainPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
